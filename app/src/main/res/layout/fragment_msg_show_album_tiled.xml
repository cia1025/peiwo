<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="me.peiwo.peiwo.fragment.MsgShowAlbumTiledFragment">

    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/album_tiled_container"
        android:layout_above="@+id/layout_handle"
        />

    <RelativeLayout
        android:id="@+id/layout_handle"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_alignParentBottom="true"
        android:background="#FAFAFA">

        <TextView
            android:id="@+id/msg_img_send"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentRight="true"
            android:gravity="center_vertical"
            android:paddingLeft="5dp"
            android:paddingRight="20dp"
            android:text="发送"
            android:textColor="#00b8d0"
            android:textSize="14sp"/>

        <TextView
            android:id="@+id/img_selected_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toLeftOf="@+id/msg_img_send"
            android:background="@drawable/green_badge"
            android:gravity="center"
            android:visibility="invisible"
            android:textColor="@color/white"/>
    </RelativeLayout>

</RelativeLayout>
